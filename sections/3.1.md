# 3.1 Wprowadzenie do RAG (Retrieval-Augmented Generation): Jak łączyć LLM z własną wiedzą?

> **Moduł 3: Budowa Aplikacji i Agenci AI**

---

## Teoria

### 1. Problem podstawowy – dlaczego same LLM nie wystarczą?

Duże modele językowe (LLM) posiadają rozległą wiedzę ogólną, ale mają trzy fundamentalne ograniczenia w zastosowaniach biznesowych:

- **Data odcięcia** (*knowledge cutoff*) – model był trenowany do określonej daty i nie zna nowszych informacji.
- **Brak wiedzy prywatnej** – model nie zna Twoich dokumentów firmowych, bazy klientów, wewnętrznej dokumentacji ani historii zamówień.
- **Halucynacje przy lukach w wiedzy** – gdy model nie zna odpowiedzi, zamiast przyznać się do niewiedzy, często generuje wiarygodnie brzmiące, lecz nieprawdziwe odpowiedzi.

**RAG (Retrieval-Augmented Generation)** to architektura, która rozwiązuje te problemy przez **dynamiczne dostarczanie modeli językowych relevantnego kontekstu** tuż przed wygenerowaniem odpowiedzi. Zamiast liczyć na to, że model „wie" wszystko, wyposażasz go w mechanizm wyszukiwania, który wyciąga potrzebne informacje z zewnętrznej bazy wiedzy i dołącza je do zapytania.

**Analogia:** Egzamin z otwartą książką. Bez RAG model jest jak student uczący się wyłącznie z pamięci – szybki, ale podatny na błędy przy niestandardowych pytaniach. Z RAG model jest jak student z dostępem do biblioteki – może sprawdzić fakty przed udzieleniem odpowiedzi, a odpowiedź jest zakorzeniona w konkretnych źródłach.

Architektura RAG stała się standardem przemysłowym dla systemów pytań i odpowiedzi (Q&A), chatbotów korporacyjnych, systemów wyszukiwania dokumentacji i asystentów obsługi klienta.

---

### 2. Architektura RAG – jak to działa krok po kroku?

Pełny pipeline RAG składa się z dwóch odrębnych faz: **indeksowania** (jednorazowe lub periodyczne) i **generowania z odzyskiwaniem** (przy każdym zapytaniu).

**Faza 1 – Indeksowanie (offline):**

```
Dokumenty źródłowe
       ↓
   Chunking (podział na fragmenty)
       ↓
   Embedding (zamiana na wektory)
       ↓
   Baza wektorowa (zapis wektorów + metadane)
```

**Faza 2 – Retrieval + Generation (na żywo):**

```
Pytanie użytkownika
       ↓
   Embedding pytania
       ↓
   Similarity search w bazie wektorowej
       ↓
   Top-K najtrafniejszych chunków
       ↓
   Augmented prompt = pytanie + chunki kontekstowe
       ↓
   LLM generuje odpowiedź opartą na kontekście
```

**Kluczowe komponenty:**

| Komponent | Rola | Przykłady |
|---|---|---|
| **Document loader** | Wczytuje dokumenty z różnych źródeł | fs, PDF, Confluence, GitHub |
| **Text splitter** | Dzieli dokumenty na chunki | RecursiveCharacterTextSplitter |
| **Embedding model** | Zamienia tekst na wektory | OpenAI `text-embedding-3-small` |
| **Vector store** | Przechowuje i przeszukuje wektory | MongoDB Atlas Vector Search |
| **Retriever** | Wyciąga relevantne chunki | similarity search, MMR |
| **LLM** | Generuje odpowiedź z kontekstem | GPT-4o, Claude 3.5 Sonnet |

---

### 3. Implementacja podstawowego RAG w Node.js z LangChain

LangChain to najpopularniejszy framework do budowania aplikacji LLM, dostępny w wersji dla Pythona i JavaScript/TypeScript. Poniżej przykład kompletnego, minimalnego RAG w Node.js:

**Instalacja:**

```bash
npm install langchain @langchain/openai @langchain/mongodb mongodb
```

**Krok 1 – Indeksowanie dokumentów:**

```javascript
// scripts/index-docs.js
const { OpenAIEmbeddings } = require('@langchain/openai');
const { MongoDBAtlasVectorSearch } = require('@langchain/mongodb');
const { RecursiveCharacterTextSplitter } = require('langchain/text_splitter');
const { TextLoader } = require('langchain/document_loaders/fs/text');
const { DirectoryLoader } = require('langchain/document_loaders/fs/directory');
const { MongoClient } = require('mongodb');

async function indexDocuments() {
  // 1. Wczytaj dokumenty z folderu
  const loader = new DirectoryLoader('./docs', {
    '.txt': (path) => new TextLoader(path),
    '.md': (path) => new TextLoader(path),
  });
  const docs = await loader.load();

  // 2. Podziel na chunki
  const splitter = new RecursiveCharacterTextSplitter({
    chunkSize: 1000,
    chunkOverlap: 200,
  });
  const chunks = await splitter.splitDocuments(docs);
  console.log(`Podzielono na ${chunks.length} chunków`);

  // 3. Zapisz w MongoDB Atlas Vector Search
  const client = new MongoClient(process.env.MONGODB_URI);
  await client.connect();
  const collection = client.db('knowledge_base').collection('documents');

  const embeddings = new OpenAIEmbeddings({ model: 'text-embedding-3-small' });
  await MongoDBAtlasVectorSearch.fromDocuments(chunks, embeddings, {
    collection,
    indexName: 'vector_index',
    textKey: 'text',
    embeddingKey: 'embedding',
  });
  console.log('Indeksowanie zakończone');
  await client.close();
}

indexDocuments();
```

**Krok 2 – Konfiguracja indeksu wektorowego w MongoDB Atlas:**

W Atlas UI przejdź do Database → Atlas Search → Create Search Index → Atlas Vector Search dla kolekcji `knowledge_base.documents`:

```json
{
  "fields": [
    {
      "numDimensions": 1536,
      "path": "embedding",
      "similarity": "cosine",
      "type": "vector"
    },
    {
      "path": "source",
      "type": "filter"
    }
  ]
}
```

**Krok 3 – Endpoint Q&A z RAG:**

```javascript
// src/routes/qa.routes.js
const { ChatOpenAI, OpenAIEmbeddings } = require('@langchain/openai');
const { MongoDBAtlasVectorSearch } = require('@langchain/mongodb');
const { createRetrievalChain } = require('langchain/chains/retrieval');
const { createStuffDocumentsChain } = require('langchain/chains/combine_documents');
const { ChatPromptTemplate } = require('@langchain/core/prompts');
const { MongoClient } = require('mongodb');

const router = require('express').Router();

const systemPrompt = `Jesteś pomocnym asystentem. Odpowiadaj na pytania wyłącznie
na podstawie dostarczonego kontekstu. Jeśli nie znasz odpowiedzi z kontekstu,
powiedz "Nie znalazłem tej informacji w dokumentacji."

Kontekst: {context}`;

router.post('/ask', async (req, res) => {
  const { question } = req.body;

  const client = new MongoClient(process.env.MONGODB_URI);
  await client.connect();
  const collection = client.db('knowledge_base').collection('documents');

  const embeddings = new OpenAIEmbeddings({ model: 'text-embedding-3-small' });
  const vectorStore = new MongoDBAtlasVectorSearch(embeddings, {
    collection,
    indexName: 'vector_index',
    textKey: 'text',
    embeddingKey: 'embedding',
  });

  const llm = new ChatOpenAI({ model: 'gpt-4o', temperature: 0 });
  const prompt = ChatPromptTemplate.fromMessages([
    ['system', systemPrompt],
    ['human', '{input}'],
  ]);

  const combineDocsChain = await createStuffDocumentsChain({ llm, prompt });
  const chain = await createRetrievalChain({
    retriever: vectorStore.asRetriever({ k: 4 }),
    combineDocsChain,
  });

  const result = await chain.invoke({ input: question });
  await client.close();
  res.json({ answer: result.answer, sources: result.context.map(d => d.metadata.source) });
});

module.exports = router;
```

---

### 4. Chunking – sztuka dzielenia dokumentów

Jakość RAG w dużej mierze zależy od tego, jak dokumenty są dzielone na fragmenty (chunki). Zbyt małe chunki tracą kontekst, zbyt duże wypełniają okno kontekstowe i zaburzają wyszukiwanie.

**Strategie chunkingu:**

| Strategia | Opis | Kiedy używać |
|---|---|---|
| **Fixed-size** | Stała liczba znaków/tokenów | Jednorodny tekst ciągły |
| **Recursive** | Dzielenie po akapitach, zdaniach, słowach | Dokumenty ogólne (najczęściej używany) |
| **Semantic** | Chunki semantyczne (granice zdaniowe + podobieństwo) | Długie artykuły, raporty |
| **Document-based** | Podział według struktury (nagłówki Markdown, sekcje HTML) | Dokumentacja techniczna, README |

**Dobre parametry startowe dla dokumentacji technicznej:**

```javascript
const splitter = new RecursiveCharacterTextSplitter({
  chunkSize: 1000,    // ~750 tokenów – mieści się w kontekście z zapasem
  chunkOverlap: 200,  // 20% nakładania – zachowuje ciągłość między chunkami
  separators: ['\n## ', '\n### ', '\n\n', '\n', ' '], // priorytety podziału
});
```

**Metadane chunków** – zawsze dołączaj informacje o źródle do każdego chunku:

```javascript
// Metadane są automatycznie dołączane przez DirectoryLoader
// ale możesz je wzbogacić:
chunks.forEach(chunk => {
  chunk.metadata.indexedAt = new Date().toISOString();
  chunk.metadata.version = '1.0';
});
```

Metadane umożliwiają cytowanie źródeł w odpowiedzi modelu i filtrowanie wyników wyszukiwania (np. „szukaj tylko w dokumentach oznaczonych tagiem `api`").

---

### 5. Ocena jakości RAG – metryki i pułapki

Zbudowanie działającego RAG to dopiero połowa sukcesu. Systemy produkcyjne wymagają systematycznej oceny jakości, zarówno na etapie retrieval jak i generowania.

**Metryki retrieval (wyszukiwania):**

- **Precision@K** – ile z K zwróconych chunków było faktycznie relevantnych dla pytania.
- **Recall** – czy wśród K chunków znalazła się informacja potrzebna do odpowiedzi.
- **MRR (Mean Reciprocal Rank)** – czy najtrafniejszy chunk był wysoko na liście.

**Metryki generowania:**

- **Faithfulness (wierność)** – czy odpowiedź modelu wynika z dostarczonego kontekstu, czy model halucynuje.
- **Answer relevance** – czy odpowiedź jest relevantna do pytania.
- **Context relevance** – czy pobrane chunki były faktycznie potrzebne.

**Narzędzia ewaluacyjne:**

```javascript
// Prosta ewaluacja manualna: zbuduj zestaw pytań z oczekiwanymi odpowiedziami
const evalSet = [
  { question: 'Jaki jest limit requestów API?', expectedAnswer: '100 req/min' },
  { question: 'Jak skonfigurować uwierzytelnianie?', expectedAnswer: 'JWT Bearer token' },
];

// Automatyczna ewaluacja przez LLM-as-judge (użyj modelu do oceny odpowiedzi)
```

**Typowe problemy i rozwiązania:**

| Problem | Przyczyna | Rozwiązanie |
|---|---|---|
| Model ignoruje kontekst | Zły format prompta | Wzmocnij instrukcję systemową |
| Złe chunki w top-K | Niespójne embeddingi | Wypróbuj MMR zamiast similarity |
| Odpowiedź z wielu źródeł niespójna | Sprzeczne dokumenty w bazie | Filtruj po wersji/dacie w metadanych |
| Wolne wyszukiwanie | Brak indeksu HNSW | Włącz indeks wektorowy w konfiguracji |

---

### 6. RAG w praktyce – integracja z istniejącą aplikacją Node.js/Angular

Zbudowanie izolowanego proof-of-concept to jedno, ale integracja RAG z istniejącą aplikacją wymaga przemyślenia kilku aspektów architektonicznych.

**Wzorzec integracji – RAG jako mikroserwis Express:**

```javascript
// Dedykowany serwis RAG oddzielony od głównej aplikacji
// docker-compose.yml

services:
  app:
    build: .
    ports: ['3000:3000']
    environment:
      - RAG_SERVICE_URL=http://rag-service:8080

  rag-service:
    build: ./rag-service
    ports: ['8080:8080']
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - MONGODB_URI=${MONGODB_URI}   # połączenie z MongoDB Atlas
```

**Komponent Angular – integracja czatu Q&A:**

```typescript
// src/app/features/docs-chat/docs-chat.component.ts
@Component({
  selector: 'app-docs-chat',
  standalone: true,
  template: `
    <div class="chat-messages">
      @for (msg of messages(); track msg.id) {
        <div [class]="'message ' + msg.role">
          <p>{{ msg.content }}</p>
          @if (msg.sources?.length) {
            <small>Źródła: {{ msg.sources.join(', ') }}</small>
          }
        </div>
      }
    </div>
    <input [(ngModel)]="question" (keyup.enter)="ask()" placeholder="Zadaj pytanie..." />
  `
})
export class DocsChatComponent {
  messages = signal<Message[]>([]);
  question = '';

  constructor(private ragService: RagService) {}

  async ask() {
    const q = this.question;
    this.question = '';
    this.messages.update(msgs => [...msgs, { id: Date.now(), role: 'user', content: q }]);
    const result = await this.ragService.ask(q);
    this.messages.update(msgs => [...msgs, { id: Date.now() + 1, role: 'assistant', ...result }]);
  }
}
```

**Koszty i optymalizacja:** Embeddingi dokumentów są jednorazowe lub periodyczne (tanie). Kosztem operacyjnym jest embedding każdego zapytania użytkownika (bardzo tanie – model `text-embedding-3-small` kosztuje ok. $0.02 za milion tokenów) i generowanie odpowiedzi przez LLM (droższa część – zależy od modelu i długości kontekstu).

---

## Zadania praktyczne

### Zadanie 1 — Łatwe: Indeksowanie dokumentów Markdown i pierwsze zapytanie

**Temat:** Utwórz bezpłatny klaster M0 w MongoDB Atlas i skonfiguruj indeks Atlas Vector Search dla kolekcji `knowledge_base.documents`. Napisz skrypt Node.js, który wczyta co najmniej 5 plików Markdown (np. dokumentację projektu lub artykuły) z lokalnego folderu, podzieli je na chunki za pomocą `RecursiveCharacterTextSplitter` i zapisze wektory do MongoDB Atlas przez `MongoDBAtlasVectorSearch` z pakietu `@langchain/mongodb`. Następnie napisz drugi skrypt, który przyjmuje pytanie z wiersza poleceń (`process.argv[2]`) i wyświetla top-3 najtrafniejszych chunków bez generowania odpowiedzi przez LLM – tylko sam retrieval. Sprawdź trafność zwracanych fragmentów dla kilku różnych pytań.

---

### Zadanie 2 — Średnie: Chatbot Q&A oparty na dokumentacji projektu z cytowaniem źródeł

**Temat:** Zbuduj endpoint REST `POST /api/ask` w Express.js, który implementuje pełny pipeline RAG: przyjmuje pytanie w formacie JSON, wyszukuje relevantne dokumenty w MongoDB Atlas Vector Search i wysyła je jako kontekst do GPT-4o lub Claude 3.5 Sonnet. Odpowiedź JSON powinna zawierać pole `answer` z treścią odpowiedzi oraz pole `sources` z listą nazw plików, z których pochodzi kontekst. Użyj instrukcji systemowej wymuszającej na modelu odpowiadanie wyłącznie na podstawie dostarczonego kontekstu i przyznawanie się do braku informacji. Przetestuj endpoint za pomocą narzędzia `curl` lub klienta HTTP.

---

### Zadanie 3 — Średnie: Porównanie strategii chunkingu na tym samym zbiorze dokumentów

**Temat:** Zindeksuj ten sam zestaw dokumentów (min. 10 plików, łącznie min. 50 KB tekstu) do trzech osobnych kolekcji w MongoDB Atlas, używając różnych konfiguracji: (A) chunkSize=500, overlap=0; (B) chunkSize=1000, overlap=200; (C) podział według nagłówków Markdown (MarkdownHeaderTextSplitter z LangChain). Przygotuj zestaw 10 testowych pytań i dla każdego sprawdź jakie chunki zwraca każda kolekcja (top-3). Sporządź tabelę porównawczą oceniając trafność retrieval dla każdej strategii i sformułuj wnioski dotyczące doboru parametrów dla różnych typów treści.

---

### Zadanie 4 — Zaawansowane: Wielosesyjny asystent dokumentacji z historią rozmowy i filtrowaniem metadanych

**Temat:** Zbuduj kompletny serwis RAG w Express.js + MongoDB z następującymi funkcjonalnościami: obsługa wielosesyjnej historii rozmowy (każda sesja przechowuje ostatnie N wiadomości w MongoDB i dołącza je do kontekstu); filtrowanie dokumentów po metadanych (np. `?category=api` zwraca odpowiedzi tylko z dokumentów oznaczonych tą kategorią — korzystając z pre-filtru w `$vectorSearch`); endpoint `POST /api/ingest` do dynamicznego dodawania nowych dokumentów do indeksu MongoDB Atlas Vector Search; endpoint `GET /api/sources` zwracający listę zaindeksowanych dokumentów z metadanymi. Zaprojektuj komponent Angular (`DocsChatComponent`) z listą konwersacji w panelu bocznym i wyświetlaniem źródeł dla każdej odpowiedzi. Skonfiguruj środowisko deweloperskie używając Docker Compose (serwis Express + lokalna instancja MongoDB do przechowywania sesji i historii) z połączeniem do MongoDB Atlas dla wyszukiwania wektorowego.
